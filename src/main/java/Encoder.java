import encryption.EncryptDecrypt;

public class Encoder implements EncryptDecrypt {
    @Override
    public char[] encrypt(char[] text) {
        return new char[0];
    }

    @Override
    public char[] decrypt(char[] text) {
        return new char[0];
    }
}
